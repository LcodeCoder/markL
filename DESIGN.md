# MarkL 视觉与交互规范


## 视觉主题

MarkL 是安静的中文写作台：工具栏退后，正文居中。品牌青蓝只用于当前文件、链接和焦点，不用大色块。

## 色彩

### 浅色

- 应用背景：`#F4F5F7`
- 目录栏：`#EEF0F3`
- 编辑纸面：`#FBFBFC`
- 主要文字：`#1C2128`
- 次要文字：`#4B5563`
- 边框：`#E8EBEF`
- 品牌/焦点：`#1B6F94`
- 选中背景：`#E4F1F6`
- 危险状态：`#C83B3B`
- 文件夹图标：`#9A7A3A`

### 深色

- 应用背景：`#22262D`
- 目录栏：`#2A2F37`
- 编辑纸面：`#2D333C`
- 主要文字：`#E8ECF1`
- 次要文字：`#B4BCC8`
- 边框：`#414854`
- 品牌/焦点：`#6FBFD9`
- 选中背景：`#2A4550`
- 危险状态：`#FF8A8A`
- 文件夹图标：`#C9A05A`

### 青雾 / 墨夜 / 海暮

- 青雾：浅色结构，背景略偏冷青
- 墨夜：比深色更暗的纸面
- 海暮：深蓝灰夜色，品牌色仍是同一家族

### 护眼 / 羊皮纸

- 桌面与侧栏用暖灰米，结构和浅色主题同一套，只是色温偏暖
- 编辑纸面：`#F7F0E4`，一点纸感高光，不要明显条纹
- 主要文字：`#2C261C`
- 次要文字：`#5A5246`
- 边框：`#D4C8B4`
- 品牌/焦点：仍是 `#1B6F94`，和浅色、深色同一家族
- 危险状态：`#B42318`
- 文件夹图标：`#9A7A3A`

所有正文、占位文本、菜单项、代码提示和状态信息必须达到可读对比度；深色主题不能遗留硬编码黑色文字。

## 字体

- 界面：`"Segoe UI Variable Text", "Segoe UI", "Microsoft YaHei UI", "PingFang SC", sans-serif`
- 编辑正文：中文无衬线，`"Source Han Sans SC", "Noto Sans SC", "PingFang SC", "Microsoft YaHei UI"`。不要用等宽字体排正文。
- 代码：`"Cascadia Code", "JetBrains Mono", Consolas, monospace`
- 正文字号约 16.5px，行高 1.88，栏宽约 1120px 居中；编辑窗口左右再各空 30px。
- 表格始终拉满正文栏宽，再按列分配宽度并换行；不要按内容收成窄条。

## 布局

- 根布局为左侧目录栏 + 右侧内容工作区。一次只编辑一篇文档。
- 目录栏最左侧是 40px 图标栏：目录、结构、搜索，只有图标没有文字。右侧才是对应内容。
- 目录栏默认宽度 280px，可拖右边在 248–480px 之间调整，宽度会记住。窄窗口改为覆盖式抽屉。
- Logo 固定在目录栏顶部，不随文件树滚动。标题栏不再重复小 Logo。
- 文件树独立滚动，底部可显示当前文件夹信息。
- 右侧包含轻量标题栏、居中正文和底部状态栏。
- 主按钮、保存等动作用描边或文字色，不用大块填充色。
- 圆角统一：控件 10px，小控件 7px，代码块与弹层 12–14px；分割线比边框更淡。

## 组件

### 目录栏

- 顶部品牌区展示带名称 Logo。
- “打开文件夹”是主要入口，配合清晰中文空状态。
- 文件夹、文件图标与顶栏按钮同一套线描：1.35px 描边、圆角接头、约 16% 浅填。闭合是带小标签的盒；展开是后袋 + 前盖。不要用实心色块。
- 文件夹可展开/折叠；Markdown 与文本文件可点击打开。
- 当前文件高亮，未保存状态在标题和文件项上显示圆点。

### 代码语言弹窗

- 输入三个反引号后，在文本光标附近出现。
- 显示中文说明和常用语言名称/别名。
- 输入字符实时筛选；方向键移动，Enter/Tab 选择，Esc 关闭。
- 弹窗遵循当前主题，层级高于编辑器。

### 工作区搜索

- 左侧图标栏的放大镜，或 `Ctrl+Shift+F`。
- 递归搜索当前文件夹及子目录：文件名、文件夹名、文档正文。
- 点文件打开；点正文结果跳到那一行；点文件夹切回目录并展开到该处。

### 快速打开

- `Ctrl+P` 在编辑区上方打开窄面板，样式接近查找条。
- 只搜当前工作区里的 Markdown / 文本，按文件名优先。
- 方向键移动，Enter 打开，Esc 关闭。没有文件夹时说明要先打开文件夹。

### 相对链接

- 文档里的 `./说明.md` 一类相对路径，点击后在工作区打开。
- `http(s)` 仍交给系统浏览器。`#标题` 跳到本文对应标题。

### 磁盘变更

- 监视当前工作区，并单独盯着当前打开的文档所在目录。自己保存的写入要忽略。
- Windows 上原子替换经常漏事件，所以当前文件还按修改时间每 400ms 轮询一次。
- 当前文件在磁盘上改过：若本地没有未保存更改，马上重载并尽量保持滚动位置；若有，问要不要用磁盘版本覆盖。
- 目录树在文件增删改后自动刷新，正在树里改名时不要打断。

### 崩溃草稿

- 有未保存内容时，把草稿记到本机用户数据目录。保存成功或用户明确放弃后清掉。
- 下次启动若草稿和磁盘不一致，用中文确认是否恢复。

### 应用内确认

放弃未保存、删除、磁盘变更、恢复草稿、报错和关于，都用编辑器里的同一块面板，不再弹出系统对话框。文件选择器仍走系统。

### 检查更新

- 不用系统对话框。新版本、已是最新、检查中、失败都走编辑区右上角的同一块面板，样式接近查找条。
- 启动后若有未忽略的新版本，只弹出这块面板，不抢编辑焦点。
- 检查更新只提示，不自动下载。绿色版优先，安装版和 GitHub Release 为次要入口。

### 选区右键

- 在正文里选一段文字，或点在一个自然段上右键，弹出图标格式条。
- 含剪切 / 复制 / 粘贴、加粗、斜体、删除线、行内代码、链接、H1–H3、引用、列表、任务和清除格式。
- 只用图标，悬停有中文说明。即时渲染和源码模式都能用。

### 查找替换

- `Ctrl+F` 打开查找条，`Ctrl+H` 同时展开替换。
- 浮在编辑区右上角，不占工具栏；即时渲染和源码模式共用同一套结果。
- 输入即搜；Enter / F3 下一个，Shift+Enter / Shift+F3 上一个，Esc 关闭。
- 「Aa」区分大小写。替换当前或全部替换。
- 打开时若正文有选区，自动填入查找框。

### 本地图片

- 粘贴截图或把图片拖入正文，保存到当前文档旁的 `assets/`，并插入相对路径。
- 未保存的新文档会先提示保存。
- 相对路径图片按文档所在目录解析，不把 `file://` 写回 Markdown。
- 导出 HTML 时把本地图内联成 `data:`，并套用当前主题颜色。

### 设置

- 顶栏在「帮助」旁边有「设置」。点开是居中弹窗，不是下拉菜单。
- 自动保存、跟随系统主题、新文档默认源码、行号用开关。
- 也可 `Ctrl+,`。

### 启动恢复

启动时恢复上次的文件夹、文档、目录栏显隐、目录栏宽度，以及窗口位置和大小。若用资源管理器双击打开文件，则以该文件为准。把 `.md` / `.txt` 拖进窗口也会打开。打开 GBK 文本时按 GBK 解码，保存仍写 UTF-8。

### 状态栏

显示当前编辑模式、中文友好的字数/字符统计、保存状态和当前文件编码提示。

## 动效

仅使用 120–180ms 的颜色、背景与折叠过渡。尊重 `prefers-reduced-motion`，不添加无功能的持续动画。

## 响应式

窗口窄于 820px 时目录栏可覆盖式展开，并提供显式切换按钮；编辑区域始终保留可操作宽度。
